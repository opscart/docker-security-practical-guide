# AKS Implementation Variant

Optional cloud-deployment evidence. Not part of the thesis — demonstrates that the same control plane operates in a real cloud environment.

**Estimated cost:** ~$3–8 per session, ~$15–30 over the lab's full run if disciplined about `terraform destroy`.

TODO: terraform module, ACR mirroring of dhi.io, AKS Workload Identity for cosign keyless verification.
